{
  "name": "mqtt-express-test",
  "version": "1.0.0",
  "description": "MQTT IoT test setup on Vercel",
  "scripts": {
    "build": "echo 'Static site with Vercel Functions - no build step required'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.0",
    "mqtt": "^5.3.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
